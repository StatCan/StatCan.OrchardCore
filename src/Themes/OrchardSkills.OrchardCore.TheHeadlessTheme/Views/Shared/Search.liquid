{% zone "Header" %}
    <header class="masthead" style="background-image: url('{{ "~/TheHeadlessTheme/img/search-bg.jpg" | href }}')">
        <div class="overlay"></div>
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-md-10 mx-auto">
                    <div class="site-heading">
                        <h1>{{ Model.ContentItem.DisplayText }}</h1>
                        <h1>Search</h1>
                    </div>
                </div>
            </div>
        </div>
    </header>
{% endzone %}

{{ Model.SearchForm | shape_render }}

{{ Model.SearchResults | shape_render }}

{{ Model.Pager | shape_render }}

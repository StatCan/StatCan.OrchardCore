<ie-banner 
  ok="{{ "OK" | t }}" 
  ie="{{ "Internet Explorer is not supported because it is a legacy browser.<br>Try a modern browser instead such as HP SureClick, Edge, Chrome, or Firefox." | t | raw }}"
>
</ie-banner>
